# Server
PORT=8080

# JWT Secret (change in production to a strong random string!)
JWT_SECRET=change-me-in-production-use-a-strong-random-secret

# Database (SQLite file path)
DB_PATH=./database.db

# OpenAI API Key (for AI Coach + Food Scanner)
OPENAI_API_KEY=sk-your-openai-api-key-here
